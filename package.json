{
  "name": "novagen-ia-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start:python\" \"npm run start:node\" \"npm run start:frontend\"",
    "start:python": "python server.py",
    "start:node": "cd server-node && node index.js",
    "start:frontend": "cd app && npm run dev",
    "install:all": "npm install && cd app && npm install && cd ../server-node && npm install",
    "dev": "npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}